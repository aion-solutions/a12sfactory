name: A12s Factory
type: profile
description: 'This distribution provides advanced features and automation for managing Websites.'
core_version_requirement: '^8.7.7 || ^9'
php: 8.0
distribution:
  name: A12S Factory

#interface translation project: a12sfactory
#interface translation server pattern: profiles/a12sfactory/translations/%language.po

dependencies:
  - drupal:block
  - drupal:breakpoint
  - drupal:ckeditor
  - drupal:config
  - drupal:config_translation
  - drupal:content_translation
  - drupal:editor
  - drupal:file
  - drupal:image
  - drupal:locale
  - drupal:node
  - drupal:options
  - drupal:text
  - drupal:user
  - drupal:views

install:
  # Core modules
  - drupal:admin_toolbar
  - drupal:admin_toolbar_tools
  - drupal:block_content
  - drupal:components
#  - drupal:contextual # @todo make this optional, but install it by default when installing the profile (manually or by CLI). Add some default permissions if enabled.
  - drupal:dblog # @todo make this optional, but install it by default when installing the profile (manually or by CLI)
  - drupal:dynamic_page_cache
  - drupal:field_ui
#  - drupal:help # @todo make this optional, but add an option when installing the profile (manually or by CLI)
  - drupal:inline_form_errors
  - drupal:layout_builder
  - drupal:media
  - drupal:menu_link_content
  - drupal:menu_ui
  - drupal:metatag # @todo make this optional, but install it by default when installing the profile (manually or by CLI)
  - drupal:page_cache
  - drupal:path
  - drupal:responsive_image
  - drupal:taxonomy
  - drupal:telephone
  - drupal:toolbar
  - drupal:views_ui

  # Contrib modules
  - ace_editor:ace_editor # @todo make this optional, but install it by default when installing the profile (manually or by CLI). Make the related filter format optional.
  - advagg:advagg # @todo make this optional, but install it by default when installing the profile (manually or by CLI)
  - advagg:advagg_css_minify # @todo make this optional, but install it by default when installing the profile (manually or by CLI)
  - advagg:advagg_js_minify # @todo make this optional, but install it by default when installing the profile (manually or by CLI)
  - auto_entitylabel:auto_entitylabel # @todo replace by ECA
  - blazy:blazy
  - block_field:block_field
  - countup:countup
  - crop:crop
  # @todo add menu_link_attributes with optional configuration?
#  - eu_cookie_compliance:eu_cookie_compliance # @todo make this optional, but add an option when installing the profile (manually or by CLI)
  - focal_point:focal_point
  - ctools:ctools
  - draggableviews:draggableviews
  - dropzonejs:dropzonejs
  - dropzonejs:dropzonejs_eb_widget
  - email_registration:email_registration # @todo make this optional, but install it by default when installing the profile (manually or by CLI)
  - embed:embed
  - entity:entity
  - entity_browser:entity_browser
  - entity_browser:entity_browser_entity_form
  - entity_browser_enhanced:entity_browser_enhanced
  - entity_embed:entity_embed
  - entity_reference_revisions:entity_reference_revisions
  - extlink:extlink
  - field_group:field_group
  - file_mdm:file_mdm
  - file_mdm:file_mdm_exif
  - focal_point:focal_point
  - honeypot:honeypot
  - icon_select:icon_select # @todo make this optional, but install it by default when installing the profile (manually or by CLI)
  - imagemagick:imagemagick # @todo make this optional, but install it by default when installing the profile (manually or by CLI)
  - inline_entity_form:inline_entity_form
  - linkit:linkit
  - maxlength:maxlength
  - menu_breadcrumb:menu_breadcrumb
  - oembed_providers:oembed_providers
  - override_node_options:override_node_options
  - paragraphs:paragraphs
  - paragraph_view_mode:paragraph_view_mode
  - password_policy:password_policy # @todo make this optional, but install it by default when installing the profile (manually or by CLI)
  - password_policy:password_policy_length # @todo make this optional, but install it by default when installing the profile (manually or by CLI)
  - password_policy:password_policy_username # @todo make this optional, but install it by default when installing the profile (manually or by CLI)
  - pathauto:pathauto
  - pathologic:pathologic
  - rabbit_hole:rabbit_hole
  - rabbit_hole:rh_node
  - rabbit_hole:rh_taxonomy
  - realname:realname
  - redirect:redirect
  - roleassign:roleassign
  - slick:slick # @todo make this optional, but install it by default when installing the profile (manually or by CLI). This implies some work as some configurations rely on this...
  - slick:slick_ui # @todo make this optional, but install it by default when installing the profile (manually or by CLI) This implies some work as some configurations rely on this...
  - slick_views:slick_views # @todo make this optional, but install it by default when installing the profile (manually or by CLI) This implies some work as some configurations rely on this...
  - smart_trim:smart_trim
  - spamspan:spamspan
  - symfony_mailer:symfony_mailer
  - taxonomy_access_fix:taxonomy_access_fix # @todo make this optional, but install it by default when installing the profile (manually or by CLI)
  - token:token
  - twig_tweak:twig_tweak
  - userprotect:userprotect
  - views_field_formatter:views_field_formatter
  - views_infinite_scroll:views_infinite_scroll
  - webform:webform
  - webform:webform_ui
  - webp:webp
  - viewsreference:viewsreference

  # Custom modules
  - a12s_layout:a12s_layout
  - a12sfactory:a12sfactory_seo # @todo make this optional, but install it by default when installing the profile (manually or by CLI)
  - a12sfactory:media_thumbnail_formatters

themes:
  - olivero
  - claro
